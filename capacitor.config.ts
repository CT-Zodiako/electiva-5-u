import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cristiantovar.guia1',
  appName: 'holamundo',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
